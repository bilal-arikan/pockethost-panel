{
  "name": "pocketbase-forge",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "PocketBaseForge — self-contained multi-tenant PocketBase host: spawns/proxies one PocketBase per subdomain. No PocketHost, no host coupling — runs anywhere in a single container.",
  "scripts": {
    "start": "bun run src/server.js"
  }
}
